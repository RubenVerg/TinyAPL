---
glyph: '⍦'
pattern: 'r←x(F⍦)y'
name: Multisets
---

`r` is the result of applying `F` to the multisets `x` and `y`: each cell of `x` and `y` is paired with its [occurrence count](./on_counts); `F` is applied; the counts are removed.