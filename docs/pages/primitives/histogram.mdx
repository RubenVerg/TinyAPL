---
glyph: '⋵'
pattern: 'r←⋵y'
name: Histogram
---

`r` is the counts in the positions indicated by the indices in `y`. It is the equivalent of `⍸⍣¯1` in other APLs. Supports the `origin` extra argument, which is used to shift the indices in the input.