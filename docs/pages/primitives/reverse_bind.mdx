---
glyph: '⍛'
pattern: 'r←({Fn}⍛{Gm})y'
name: Reverse Bind
---

Exactly one of the operands is an array and exactly one of the operands is a function. If `n` is provided, `r` is `y G n`; if `m` is provided, `r` is `m F y`.