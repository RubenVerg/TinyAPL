---
glyph: '◠'
pattern: 'r←[x](F◠)y'
name: On Scalars
---

`r` is the result of applying `F` to scalars of \[`x` and\] `y`: `r←[x] F⍤0 y`. Supports the `fill` extra argument, which is used to pad the results of `F` to have matching shape.