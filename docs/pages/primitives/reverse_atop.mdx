---
glyph: '⍛'
pattern: 'r←[x](F⍛G)y'
name: Reverse Atop
---

If `x` is not provided, `r` is `G (F y)`; if `x` is provided, `r` is `G (x F y)`.

|Monad|Dyad|
|:----|:---|
|<combinator key="reverse_compose" name="Reverse Compose" />|<combinator key="reverse_atop" name="Reverse Atop" />|