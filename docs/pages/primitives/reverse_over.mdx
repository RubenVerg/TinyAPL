---
glyph: '⍜'
pattern: 'r←[x](F⍜G)y'
name: Reverse Over
---

If `x` is not provided, `r` is `G (F y)`; if `x` is provided, `r` is `(F x) G (F y)`.

|Monad|Dyad|
|:----|:---|
|<combinator key="reverse_compose" name="Reverse Compose" />|<combinator key="reverse_over" name="Reverse Over" />|