---
glyph: '◡'
pattern: 'r←[x](F◡)y'
name: On Cells
---

`r` is the result of applying `F` to major cells of \[`x` and\] `y`: `r←[x] F⍤¯1 y`. Supports the `fill` extra argument, which is used to pad the results of `F` to have matching shape.