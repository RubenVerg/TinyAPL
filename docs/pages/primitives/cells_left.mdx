---
glyph: 'ᓗ'
pattern: 'r←x(Fᓗ)y'
name: Cells Left
---

`r` is the result of calling `F` on each major cell of `x` and `y` as a whole: `r←x F⍤¯1‿∞ y`. Supports the `fill` extra argument, which is used to pad the results of `F` to have matching shape.