---
glyph: 'ᑒ'
pattern: 'r←[x](Fᑒ)y'
name: On Contents
---

`r` is the result of applying `F` to the disclose of \[`x` and\] `y`: `r←[x] F⍥⊃ y`. Supports the `fill` extra argument, which is returned when an argument is empty.