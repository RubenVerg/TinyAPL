---
glyph: 'ᓚ'
pattern: 'r←x(Fᓚ)y'
name: Cells Right
---

`r` is the result of calling `F` on `x` as a whole and each major cell of `y`: `r←x F⍤∞‿¯1 y`. Supports the `fill` extra argument, which is used to pad the results of `F` to have matching shape.