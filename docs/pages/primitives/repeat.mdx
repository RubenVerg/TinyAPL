---
glyph: '⍣'
pattern: 'r←[x](F⍣m)y'
name: Repeat
---

If `m` is positive, `r` is the result of calling \[`x∘`\]`F` `m` times, starting from `y`. If `m` is negative, `r` is the result of calling \[`x∘`\]`F˝` `-m` times, starting from `y`.