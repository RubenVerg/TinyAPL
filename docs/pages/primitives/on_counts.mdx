---
glyph: '⍦'
pattern: 'r←(F⍦)y'
name: On Counts
---

`r` is the result of applying `F` the occurrence count of the major cells of `y`, which is defined as a vector of the same length as `y` where each element is the amount of times the corresponding cell of `y` appears in the array before that place.