---
glyph: '='
pattern: 'r←=y'
name: Classify
---

`r` is the classification of the major cells of `y`: for each major cell, the index of that cell in the unique major cells of `y`. Equivalent to `(∪y)⍳y`. Supports the `tolerance`, `origin`, `backward`, and `fill` extra arguments.
