---
glyph: '/'
pattern: 'r←(F/)y'
name: Reduce
---

`r` is the result of reducing the major cells of `y` using `F`, left-to-right. Supports the `backward` extra argument, which reduces right-to-left.