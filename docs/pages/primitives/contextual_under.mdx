---
glyph: '⍫'
pattern: 'r←[x]({Fn}⍫G)y'
name: Contextual Under
---

`G` is a monadic function with a contextual under. `r` is the result of applying `G` storing context, calling \[`x∘`\]`F`, or `n⍨` on the result and then undoing `G` with the context.