---
glyph: ','
pattern: 'r←x,y'
name: Laminate
---

`r` is an array whose major cells are `x` and `y`: `r←x⍪⍥∧y`. Supports the `fill` extra argument, which is used to pad the two arrays to have matching shape.