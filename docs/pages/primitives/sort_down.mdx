---
glyph: '⊵'
pattern: 'r←⊵y'
name: Sort Down
---

`r` is the descending sort of the major cells of `y`: `r←y⍒y`. Supports the `tolerance` extra argument for the comparisons; supports the `backward` extra argument, which returns the&nbsp;<primitive key="sort_up" />&nbsp;of `y`.