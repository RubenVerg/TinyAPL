---
glyph: '⋷'
pattern: 'r←x⋷y'
name: Mask
---

`r` is a natural array with the same shape as `y`; each occurence of `x` in `y` is marked with an unique number in all positions; overlapping matches are ignored. Supports the `tolerance` extra argument for the comparisons.